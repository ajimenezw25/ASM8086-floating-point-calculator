# ASM8086-floating-point-calculator
CalcuTec is a console-based calculator written in 8086 Assembly (16-bit) using Turbo Assembler (TASM). The program is designed to run in DOS real mode and relies on DOS interrupts (int 21h) for keyboard input and screen output.
